#include <stdio.h>
void main()
{
  int n,ele,i,sh,j;
  sh=j=0;
  printf("enter the value of n");
  scanf("%d",&n);
  i=1;
  while(i<=n)
  {
    scanf("%d",&ele);
    if(ele>j)
    {
      sh=j;
      j=ele;
    }
    else if(ele>sh)
    sh=ele;
    i++;
  }
  printf("second highest = %d",sh);
}
